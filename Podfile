# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Example For MyFramework' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!
pod 'MyFramework', :path => '.'
  # Pods for Example For MyFramework

  target 'Example For MyFrameworkTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Example For MyFrameworkUITests' do
    # Pods for testing
  end

end

target 'MyFramework' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyFramework

end
